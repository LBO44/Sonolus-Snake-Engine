import { InstructionIconName } from "@sonolus/core";

export const instruction = defineInstruction({
    texts: {
        movement: 'Swipe to change direction or tap the Dpad (enable it in config)',
        apple: 'Eat an apple to grow and increase your score',
        death: "Don't bump your head against the wall or yourself",
    },
    icons: {
    },
})
